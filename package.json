{
  "name": "barzo",
  "version": "1.0.0",
  "description": "A modern messaging application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npm run build && nodemon server.js",
    "build": "tailwindcss -i ./public/src/styles/main.css -o ./public/dist/styles.css --minify",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "worker:dev": "wrangler dev worker/index.js",
    "worker:deploy": "wrangler deploy worker/index.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "eslint": "^8.55.0",
    "nodemon": "^3.0.2",
    "postcss": "^8.5.1",
    "tailwindcss": "^3.4.17",
    "wrangler": "^3.28.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
